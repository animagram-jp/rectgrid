[package]
name = "rectgrid"
version = "0.1.0"
edition = "2024"
description = "Region operations on rectilinear grids with arbitrary unit systems."
license = "Apache-2.0"
categories  = ["no-std", "wasm"]
exclude = ["examples/"]

[dependencies]
libm = "0.2"